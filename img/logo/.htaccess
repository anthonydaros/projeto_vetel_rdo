# Allow public access to logo images
Options +Indexes
Allow from all
Require all granted

# Allow specific image file types
<FilesMatch "\.(jpg|jpeg|png|gif|bmp|webp|svg|05)$">
    Order Allow,Deny
    Allow from all
    Require all granted
</FilesMatch>

# Set proper headers for images
<IfModule mod_headers.c>
    <FilesMatch "\.(jpg|jpeg|png|gif|bmp|webp|svg|05)$">
        Header set Access-Control-Allow-Origin "*"
        Header set Cache-Control "public, max-age=31536000"
    </FilesMatch>
</IfModule>